## packages required at runtime but never referenced directly in app code
## renv scans this file to keep them in renv.lock; it is never sourced

library(signal)  ## optional dependency of {mnirs}, used for filtering
